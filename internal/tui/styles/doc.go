// Package styles defines the semantic colour tokens every other view package
// renders through, so a theme change is one edit rather than a search for
// hard-coded colours.
//
// Does not contain: layout, widgets, or any Bubble Tea model. It is data.
package styles
